// Generated by IcedCoffeeScript 1.6.3-e
(function() {
  var Attribute, Constants, Cypher, CypherBuilder, CypherLinkUtil, Data, DataRoute, Entity, EntityUtil, ErrorDevMessage, Link, Logger, Neo, Permission, Remote, Response, Tag, User, Vote, redis, _show, _und;



  _und = require('underscore');

  redis = require('../models/setup').db.redis;

  Logger = require('util');

  Remote = require('../remote/remote');

  Neo = require('../models/neo');

  User = require('../models/user');

  Entity = require('../models/entity');

  Attribute = require('../models/attribute');

  Data = require('../models/data');

  Tag = require('../models/tag');

  Vote = require('../models/vote');

  Link = require('../models/link');

  Constants = require('../config').Constants;

  EntityUtil = require('./entity/util');

  DataRoute = require('./data');

  Cypher = require('./util/cypher');

  CypherBuilder = Cypher.CypherBuilder;

  CypherLinkUtil = Cypher.CypherLinkUtil;

  Response = require('./util/response');

  ErrorDevMessage = Response.ErrorDevMessage;

  Permission = require('./permission');

  _show = function(req, res, next) {
    var data, entityId;
    entityId = req.query.entity;
    data = req.query.data;
    data = req.query.data;
    return Response.OKResponse(res)(200);
  };

  exports.show = _show;

}).call(this);
