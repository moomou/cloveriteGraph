// Generated by IcedCoffeeScript 1.6.2d
(function() {
  var Contants, INDEX_NAME, Indexes, Link, LinkSchema, Neo, Setup, StdSchema, redis, _und;



  _und = require('underscore');

  Setup = require('./setup');

  Neo = require('./neo');

  redis = Setup.db.redis;

  StdSchema = require('./stdSchema');

  Contants = StdSchema.Contants;

  INDEX_NAME = 'rLink';

  Indexes = [
    {
      INDEX_NAME: INDEX_NAME,
      INDEX_KEY: 'name',
      INDEX_VALUE: ''
    }, {
      INDEX_NAME: INDEX_NAME,
      INDEX_KEY: 'veracity',
      INDEX_VALUE: ''
    }
  ];

  LinkSchema = {
    veracity: 0,
    createdAt: -1,
    modifiedAt: -1,
    "private": false,
    version: 0
  };

  module.exports = Link = (function() {
    function Link(data) {
      var validKeys;
      validKeys = _und.keys(LinkSchema);
      _und.defaults(data, LinkSchema);
      this.name = this.normalizeName(data['name']);
      this.data = data;
    }

    Link.prototype.normalizeName = function(name) {
      var normalized;
      return normalized = "_" + (name.toUpperCase());
    };

    return Link;

  })();

  /*
  Static Method
  */


  Link.Name = 'rLink';

  Link.INDEX_NAME = INDEX_NAME;

}).call(this);

/*
//@ sourceMappingURL=link.map
*/
