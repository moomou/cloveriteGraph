// Generated by IcedCoffeeScript 1.6.2d
(function() {
  var Contants, StdSchema, VoteLink, VoteSchema, _und;



  _und = require('underscore');

  StdSchema = require('./stdSchema');

  Contants = StdSchema.Contants;

  VoteSchema = {
    type: '',
    user: '',
    time: '',
    ipAddr: '',
    lang: '',
    browser: '',
    os: '',
    rating: 0
  };

  module.exports = VoteLink = (function() {
    function VoteLink(vote) {
      this.data = _und.clone(vote);
      this.name = '_VOTE';
      _und.defaults(this.data, VoteSchema);
      if (!this.data.time) {
        this.data.time = '' + new Date().getTime();
      }
    }

    return VoteLink;

  })();

}).call(this);
